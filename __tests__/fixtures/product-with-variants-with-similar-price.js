import { decimalPrice } from '../../src/lib/decimal-price'

const ProductsWithVariantsWithSimilarPrice = {
  min_current_price: decimalPrice(10),
  max_current_price: decimalPrice(10)
}

export default ProductsWithVariantsWithSimilarPrice
